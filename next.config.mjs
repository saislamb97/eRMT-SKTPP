/** @type {import('next').NextConfig} */
const nextConfig = {
  // `appDir` is enabled by default in recent Next.js versions (v14+).
  // Remove deprecated/experimental flags to avoid invalid-config warnings.
};

export default nextConfig;

